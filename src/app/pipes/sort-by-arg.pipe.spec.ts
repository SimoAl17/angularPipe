import { SortByArgPipe } from './sort-by-arg.pipe';

describe('SortByArgPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByArgPipe();
    expect(pipe).toBeTruthy();
  });
});
