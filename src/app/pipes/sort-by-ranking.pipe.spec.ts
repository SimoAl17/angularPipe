import { SortByRankingPipe } from './sort-by-ranking.pipe';

describe('SortByRankingPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByRankingPipe();
    expect(pipe).toBeTruthy();
  });
});
